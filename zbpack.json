{
  "build_command": "cd frontend && npm install && npm run build && cd .. && mkdir -p static && cp -r frontend/dist/* static/ && cd backend && cargo build --release",
  "start_command": "./backend"
}
